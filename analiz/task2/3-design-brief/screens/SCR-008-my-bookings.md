# Мои записи

**ID:** SCR-008  
**Тип:** Экран  
**Домен:** 03. Мои записи  
**Приоритет:** Critical  
**Статус:** Актуален  
**Сессия клиента:** ClientSession — Bearer после первой записи  
**Дизайн-макет:** Figma — TBD · **Design brief:** [SCR-008-my-bookings.md](SCR-008-my-bookings.md)

> **Платформа:** Android (NFR-001) · **Язык UI:** только русский (NFR-008) · **Оплата:** на месте (FR-015).

---

## Содержание

- [Обзор](#обзор)
- [Навигация](#навигация)
- [Входные данные](#входные-данные)
- [Применяемые логики](#применяемые-логики)
- [Инициализация](#инициализация)
- [Используемые запросы](#используемые-запросы)
- [Макет экрана](#макет-экрана)
- [Элементы экрана](#элементы-экрана)
- [Состояния экрана](#состояния-экрана)
- [Связанные требования](#связанные-требования)
- [Критерии приёмки](#критерии-приёмки)

---

## Обзор

Экран показывает список записей текущего клиента и разделяет его на сегменты «Предстоящие» и «Прошедшие». Данные загружаются через `listBookings` под `ClientSession`, а при отсутствии сети используется локальный кэш последнего успешного ответа (NFR-009).

### User Story

> Как клиент, я хочу видеть свои предстоящие и прошедшие записи, чтобы контролировать посещения и быстро открывать детали нужной брони.

**Не в MVP:** лист ожидания при `NO_SPOTS` (FR-011) — только возврат к расписанию; фильтр по шефу; iOS.

---

## Навигация

### Входящая

| Источник | Триггер | Условие | Параметры |
| :-- | :-- | :-- | :-- |
| SCR-001 | Тап по вкладке «Мои записи» | Всегда | — |
| SCR-006 | CTA «К моим записям» | После успешного бронирования | — |
| SCR-010 | Закрытие после успешной отмены | `cancelBooking = 200` | `refresh=true` |
| SCR-009 | Системная кнопка «Назад» | Открытие детали из списка | Сохранение позиции скролла |

### Исходящая

| Назначение | Триггер | Параметры |
| :-- | :-- | :-- |
| SCR-009 | Тап по карточке записи | `bookingId` |
| SCR-001 | CTA из empty state «Посмотреть расписание» | — |

> **Нижняя навигация:** 2 вкладки — «Расписание» (SCR-001) | «Мои записи» (SCR-008). Отдельной вкладки «Профиль» нет.

---

## Входные данные

| Название | Тип | Источник | Описание |
| :-- | :-- | :-- | :-- |
| `sessionToken` | string | ClientSession | Bearer-токен текущего клиента |
| `refresh` | boolean | Навигация | Флаг принудительного обновления после возврата |
| `selectedSegment` | enum(`UPCOMING`,`PAST`) | Локальный UI state | Активный сегмент списка |

---

## Применяемые логики

| Логика | Элемент / триггер | Описание |
| :-- | :-- | :-- |
| [LOGIC-008](../../5-mobile-app-spec/09_Логики/LOGIC-008_Паттерн-состояний-экрана.md) | Loading/Content/Empty/Error/Offline | Единый паттерн состояний экрана |
| [LOGIC-003](../../5-mobile-app-spec/09_Логики/LOGIC-003_Цена-класса.md) | Цена в карточке | Отображение `totalPrice`, прокат не влияет на сумму |
| [LOGIC-004](../../5-mobile-app-spec/09_Логики/LOGIC-004_Отмена-правило-3-часов.md) | Возврат после отмены | После cancel запись меняет статус на `CANCELLED_BY_CLIENT` |

---

## Инициализация

### Запросы при открытии

| № | operationId | Критичный | Условие |
| :-: | :-- | :--: | :-- |
| 1 | `listBookings` | Да | Первое открытие экрана или `refresh=true` |

> Сегментация «Предстоящие/Прошедшие» выполняется на клиенте по `slot.startsAt` и статусу.

---

## Используемые запросы

### listBookings

**Метод:** GET  
**Путь:** `/bookings`  
**Спецификация:** [../../api/openapi.yaml](../../api/openapi.yaml) → `listBookings`

**Обработка ответа:**

| HTTP / код | UI-реакция |
| :-- | :-- |
| 200 + `items.length > 0` | Content, сохранение в офлайн-кэш |
| 200 + `items.length = 0` | Empty (текст зависит от активного сегмента) |
| 401 | Завершение сессии, переход в поток первичной идентификации |
| 5xx / timeout | Error + «Повторить» |
| offline + есть кэш | Offline state + баннер «Показаны сохранённые данные» |

---

## Макет экрана

```
┌─────────────────────────────────────┐
│ Мои записи                          │
├─────────────────────────────────────┤
│ [Предстоящие] [Прошедшие]           │
├─────────────────────────────────────┤
│ ⚠ Нет сети. Показаны сохранённые    │  (только в Offline)
│   данные                            │
├─────────────────────────────────────┤
│ ┌─────────────────────────────────┐ │
│ │ Ср, 9 июл · 18:00              │ │
│ │ Паста и соусы                  │ │
│ │ Шеф: Мария С.                  │ │
│ │ 3 500 ₽          [ЗАПИСАН]     │ │
│ └─────────────────────────────────┘ │
│ ...                                 │
├─────────────────────────────────────┤
│ [Расписание]        [Мои записи]    │
└─────────────────────────────────────┘
```

---

## Элементы экрана

| Элемент | Описание | Источник данных | Валидация / поведение |
| :-- | :-- | :-- | :-- |
| Сегмент «Предстоящие/Прошедшие» | Локальное переключение двух списков | UI state + `listBookings.items` | Не вызывает повторный API-запрос |
| Карточка записи | Дата, время, программа, шеф, цена, статус | `slot.startsAt`, `slot.program.name`, `slot.chef.fullName`, `totalPrice`, `status` | Вся карточка кликабельна, переход на SCR-009 |
| Бейдж статуса | Текстовое состояние записи | `status` | `ACTIVE`→«Записан», `CANCELLED_BY_CLIENT`→«Отменена вами», `CANCELLED_BY_STUDIO`→«Отменена студией», `ATTENDED`→«Посещена» |
| Офлайн-баннер | Сообщение о кэше | Статус сети + timestamp кэша | Показывается только в offline режиме |
| Empty state | Сообщение об отсутствии записей | Пустой список в активном сегменте | Для «Предстоящие» CTA на SCR-001 |
| Pull-to-refresh | Обновление данных | Жест пользователя | Активен только online |

**Терминология:** **шеф**, **программа класса**, **тип кухни**; прокат (фартук, ножи) **не влияет на цену** (FR-015).

---

## Состояния экрана

| Состояние | Условие | Отображение |
| :-- | :-- | :-- |
| Loading | Первый запрос в процессе | Skeleton карточек |
| Content | 200 + записи есть в выбранном сегменте | Список карточек |
| Empty | 200 + выбранный сегмент пуст | Текст + CTA (для «Предстоящие») |
| Error | 4xx / 5xx на первой загрузке | Ошибка + «Повторить» |
| Offline | Нет сети, кэш есть | Кэшированный список + офлайн-баннер |
| Refreshing | Pull-to-refresh online | Индикатор обновления поверх списка |

---

## Связанные требования

| ID | Связь |
| :-- | :-- |
| UC-003 | Просмотр списка собственных записей |
| FR-016 | Статусы и список броней клиента |
| FR-015 | Показ стоимости класса, оплата на месте |
| NFR-009 | Офлайн-кэш для SCR-008 |
| NFR-008 | Все пользовательские тексты на русском |
| NFR-001 | Android-платформа |

---

## Критерии приёмки

| ID | Критерий |
| :-- | :-- |
| AC-001 | **Дано** открыта вкладка «Мои записи» online, **Когда** `listBookings` возвращает список, **Тогда** показывается сегмент «Предстоящие» по умолчанию и карточки ближайших записей. |
| AC-002 | **Дано** в активном сегменте нет записей, **Когда** экран перешёл в Empty, **Тогда** показывается релевантный текст и CTA «Посмотреть расписание» для «Предстоящие». |
| AC-003 | **Дано** пользователь переключил сегмент, **Когда** меняется `selectedSegment`, **Тогда** список фильтруется локально без повторного вызова `listBookings`. |
| AC-004 | **Дано** устройство offline и есть кэш списка, **Когда** экран открыт, **Тогда** показываются сохранённые данные и баннер «Показаны сохранённые данные». |
| AC-005 | **Дано** выполнен tap по карточке записи, **Когда** передан `bookingId`, **Тогда** открывается SCR-009 с деталями выбранной записи. |
| AC-006 | **Дано** пользователь вернулся из SCR-010 после успешной отмены, **Когда** экран получает `refresh=true`, **Тогда** карточка отменённой записи показывает статус «Отменена вами». |
| AC-007 | **Дано** на карточке отображается цена и выбран прокат, **Когда** строится UI, **Тогда** сумма берётся из API и не меняется из-за проката. |
